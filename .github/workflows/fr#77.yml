name: testingfr#79
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4


      - name: Set up python 
        uses: gowridurgad/setup-python@pip-install
        with:
          
          pip-install: '-r requirements.dev.txt'
              
