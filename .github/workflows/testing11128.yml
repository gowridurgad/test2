name: main

on:
  workflow_dispatch:

jobs:
  reproduce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: ls -la
      - run: cat requirements.txt

      - uses: gowridurgad/sample-python@main

      - run: ls -la
      - run: cat temp/requirements.txt
      

  
